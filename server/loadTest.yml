config:
  target: 'http://localhost:4000'
  # http: 
  #   pool: 10
  phases:
    - duration: 60
      arrivalRate: 135
  defaults:
    headers:
      x-my-service-auth: '987401838271002188298567'
scenarios:
  - flow:
    - loop:
      - get:
          url: "/api/products"
      count: 5